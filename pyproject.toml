[project]
name = "cyclops"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Tavish", email = "tavishdhawan3103@gmail.com" }
]
requires-python = ">=3.14"
dependencies = []

[project.scripts]
cyclops = "cyclops:main"

[build-system]
requires = ["uv_build>=0.12.2,<0.13.0"]
build-backend = "uv_build"
